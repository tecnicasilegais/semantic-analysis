int num;
int result;
int i;
int atual;

void main() { 
  while (true) {
    write(" - - - - - - - - ");
    write("Informe um numero: ");
    read( num );

		if( num < 10 ) {
			write("Numero deve ser no minimo 10");
			break;
		}

		i = 1;
    		
    while (i <= num) {
    	atual = i++;

			if( atual % 2 != 0) continue;

    	write("Numero par -> ", atual);
    				
    }
  }    
} 

